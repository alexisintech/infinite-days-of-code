// ~ * ~ * ~ * ~ * ~ * ~ * ~ * ~ * ~ * ~ *
//////////////////////////////////////////
// CLASS 29 - 04262022 
//////////////////////////////////////////
// ~ * ~ * ~ * ~ * ~ * ~ * ~ * ~ * ~ * ~ *


/* Instructions: Your pokemon party order which is a list of pokemon has been leaked to Misty. 
Please create a function that reverses your list and prints it to the console.
*/

// ~*~*~*~*~*~* My Solution

function reverseList(arr) {
    console.log(arr.reverse());
}

// ~*~*~*~*~*~* Leon's Solution

function reverseParty(partyList) {
    console.log( partyList.reverse() );
}

reverseParty(['bulba', 'char', 'pika', 'butter']);